# Spindle docs

Start here if you are new to the repository.

## Read first

- [`spindle-architecture.md`](spindle-architecture.md) — quick-start tour of
  the five-crate workspace, the validator-oriented architecture pillars, and
  the dependency direction between crates.
- [`spindle-implementation-brief.md`](spindle-implementation-brief.md) —
  current architectural brief and contributor orientation.

## Operator and user docs

- [`spindle-agent-config.md`](spindle-agent-config.md) — runtime model-agent
  configuration (`spindle.toml`, `SPINDLE_CONFIG`).
- [`spindle-harness-usage.md`](spindle-harness-usage.md) — operator workflow
  for the `spindle-harness` batch drafting CLI.
- [`spindle-harness-seed.example.json`](spindle-harness-seed.example.json) —
  sample harness seed file referenced from the usage doc.
- [`spindle-skills/`](spindle-skills/) — note on where to edit skill prompts
  (the canonical source is the root `skills/` directory).
