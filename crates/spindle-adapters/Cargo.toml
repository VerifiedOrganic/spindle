[package]
name = "spindle-adapters"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Bounded-concurrency fan-out (`stream::buffered`) for the deep-check tiers.
futures-util = "0.3.31"
anyhow.workspace = true
base64.workspace = true
chrono.workspace = true
dirs.workspace = true
html2text.workspace = true
reqwest.workspace = true
roxmltree.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
spindle-core = { path = "../spindle-core" }
spindle-skills = { path = "../spindle-skills" }
# Phase 6: SurrealDB removed. The SQLite stack is the sole persistence
# layer; the surrealdb crate is no longer a dep.
rusqlite.workspace = true
tokio-rusqlite.workspace = true
refinery.workspace = true
sqlite-vec.workspace = true
ulid.workspace = true
tempfile.workspace = true
thiserror.workspace = true
tokio.workspace = true
toml.workspace = true
tracing.workspace = true
zip.workspace = true

[dev-dependencies]
tempfile.workspace = true

[features]
perf = []
# Exposes test-only seams (the ModelRouter dispatch recorder, evolution §4
# rule 2) so downstream crates' integration tests can observe post-gate
# dispatches. Off in normal builds — zero production cost.
test-support = []
